<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <%= favicon_link_tag asset_path('cake.png'), :rel => 'icon', :type =>  'image/png' %>
  <title>Ali-Bon-Bon</title>
  <link href='https://fonts.googleapis.com/css?family=Berkshire Swash' rel='stylesheet'>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <section class="image header">
      <h2>
        <%= link_to "Ali-Bon-Bon : Treat Yo Self!", root_path %>
      </h2>
    </section>
    <!-- FOUNDATION STYLING FOR DROP DOWN MENUS -->
    <div id="nav-bar float-left">
      <h3 class= "menu toggle"></h3>
      <nav>
        <div>
          <ul class="menu toggle" data-dropdown-menu>
            <li>
              <a class="dropdown-1">Browse</a>
              <ul class="menu">
                <li><a href="/products">All Products</a></li>
                <li>
                  <a href="/categories">All Products By Category</a>
                </li>
                <li>
                  <a href="/merchants">All Products By Baker</a>
                </li>
              </ul>
            </li>
          </ul>

          <div class="user-nav columns large-3 small-12 float-right">

            <% if session[:cart_id] %>
            <%= link_to "My Cart", cart_path(session[:cart_id], class: "hollow button") %>
            <% else %>
            <a class= "hollow button">My Cart</a>
            <% end %>

            <% if session[:merchant_id] %>
            <%= link_to "Log Out", logout_path, method: :delete, class: "hollow button" %>
            <%= link_to "Logged in as #{Merchant.find(session[:merchant_id]).username}", merchant_path(session[:merchant_id]), class: "button" %>
            <% else %>
            <%= link_to "Log In", "/auth/github", class: "warning button" %>
            <% end %>



          </div>
        </div>
      </nav>
    </div>
  </header>

  <% if flash[:result_text] or flash[:messages] %>
  <section class="status <%= flash[:status] %>">
    <h3><%= flash[:status] == :failure ? "A problem occurred: " : "" %><%= flash[:result_text] %></h3>
    <% if flash[:messages] %>
    <ul>
      <% flash[:messages].each do |name, problems| %>
      <% problems.each do |problem| %>
      <li><%= name %>: <%= problem %></li>
      <% end %>
      <% end %>
    </ul>
    <% end %>
  </section>
  <% end %>

  <main>

    <%= yield %>
  </main>

</body>
</html>
