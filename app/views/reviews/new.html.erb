<h1>Write a Review for <%= @product.name %></h1>
<%= @review = Review.new %>
<%= form_for [@product, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.select :rating, ratings_options %>

  <%= f.label :review %>
  <%= f.text_area :review %>

  <%= f.submit %>
<% end %>
